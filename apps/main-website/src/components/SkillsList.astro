---
interface Skill {
  name: string;
  category: "frontend" | "backend" | "tooling" | "other";
}

interface Properties {
  skills?: Skill[];
}

const defaultSkills: Skill[] = [
  { name: "React", category: "frontend" },
  { name: "Astro", category: "frontend" },
  { name: "TypeScript", category: "frontend" },
  { name: "CSS/Vanilla CSS", category: "frontend" },
  { name: "Node.js", category: "backend" },
  { name: "Express", category: "backend" },
  { name: "Vite", category: "tooling" },
  { name: "Turbo", category: "tooling" },
  { name: "Git", category: "tooling" },
  { name: "Accessibility (a11y)", category: "other" },
  { name: "Responsive Design", category: "other" },
];

const { skills = defaultSkills } = Astro.props;

const categories = [
  { key: "frontend", label: "Frontend" },
  { key: "backend", label: "Backend" },
  { key: "tooling", label: "Tooling" },
  { key: "other", label: "Other" },
] as const;

const grouped = categories
  .map(cat => ({
    ...cat,
    skills: skills.filter((s: Skill) => s.category === cat.key),
  }))
  .filter(g => g.skills.length > 0);
---

<div class="skills-list">
  {
    grouped.map(group => (
      <div class="skills-list__group">
        <span class="skills-list__label">{group.label}</span>
        <div class="skills-list__pills">
          {group.skills.map((skill: Skill) => (
            <span class={`skills-list__pill skills-list__pill--${group.key}`}>
              {skill.name}
            </span>
          ))}
        </div>
      </div>
    ))
  }
</div>

<style>
  .skills-list {
    /* Layout */
    display: flex;
    flex-direction: column;
    gap: var(--spacing-24);
  }

  .skills-list__group {
    /* Layout */
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-16);
  }

  .skills-list__label {
    /* Skin */
    font-size: var(--font-size-100);
    font-family: var(--font-family-mono);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: var(--font-letter-spacing-wide);
    color: var(--color-text-secondary);

    /* Layout */
    flex-shrink: 0;
    width: 80px;
    padding-top: calc(var(--spacing-4) + 2px);
  }

  .skills-list__pills {
    /* Layout */
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-8);
  }

  .skills-list__pill {
    /* Skin */
    font-size: var(--font-size-200);
    font-weight: var(--font-weight-medium);
    font-family: var(--font-family-mono);
    letter-spacing: 0.02em;
    border: 1px solid transparent;
    transition: transform 0.15s ease, box-shadow 0.15s ease;

    /* Layout */
    display: inline-flex;
    align-items: center;
    padding: var(--spacing-4) var(--spacing-12);
    border-radius: var(--border-radius-pill);
  }

  .skills-list__pill:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow-card);
  }

  /* DS NOTE: --color-skill-frontend candidate */
  .skills-list__pill--frontend {
    background: color-mix(
      in srgb,
      var(--color-primary) 10%,
      var(--color-background-secondary)
    );
    border-color: color-mix(in srgb, var(--color-primary) 25%, transparent);
    color: var(--color-primary);
  }

  /* DS NOTE: --color-skill-backend candidate */
  .skills-list__pill--backend {
    background: color-mix(
      in srgb,
      var(--color-success) 10%,
      var(--color-background-secondary)
    );
    border-color: color-mix(in srgb, var(--color-success) 25%, transparent);
    color: var(--color-base-green-500);
  }

  /* DS NOTE: --color-skill-tooling candidate */
  .skills-list__pill--tooling {
    background: color-mix(
      in srgb,
      var(--color-text-secondary) 10%,
      var(--color-background-primary)
    );
    border-color: color-mix(
      in srgb,
      var(--color-text-secondary) 25%,
      transparent
    );
    color: var(--color-text-secondary);
  }

  .skills-list__pill--other {
    background: var(--color-background-secondary);
    border-color: color-mix(
      in srgb,
      var(--color-text-primary) 12%,
      transparent
    );
    color: var(--color-text-primary);
  }

  @media (max-width: 600px) {
    .skills-list__group {
      flex-direction: column;
      gap: var(--spacing-8);
    }

    .skills-list__label {
      width: auto;
    }
  }
</style>
