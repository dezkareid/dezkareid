---
phase: 02-core-web-rules
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [src/configs/recommended.ts, src/configs/strict.ts, src/index.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "recommended config includes core rules as warn"
    - "strict config includes core rules as error"
    - "@eslint/css baseline is integrated"
  artifacts:
    - path: "src/configs/recommended.ts"
      provides: "Recommended preset"
    - path: "src/configs/strict.ts"
      provides: "Strict preset"
---

<objective>
Create configuration presets and integrate CSS baseline.

Purpose: Provide easy-to-use bundles for developers.
Output: 2 config files.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-web-rules/02-CONTEXT.md
@.planning/phases/02-core-web-rules/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recommended config</name>
  <files>src/configs/recommended.ts</files>
  <action>
    - Define `recommended` object.
    - Set core rules to `"warn"`.
    - Spread `@eslint/css.configs.baseline.rules`.
    - Define default glob patterns for HTML and JSX.
  </action>
  <verify>Check file content</verify>
  <done>Recommended config is created</done>
</task>

<task type="auto">
  <name>Task 2: Create strict config</name>
  <files>src/configs/strict.ts</files>
  <action>
    - Define `strict` object.
    - Set core rules to `"error"`.
    - Include same CSS integration.
  </action>
  <verify>Check file content</verify>
  <done>Strict config is created</done>
</task>

<task type="auto">
  <name>Task 3: Export configs in index.ts</name>
  <files>src/index.ts</files>
  <action>
    Add `configs` key to plugin object.
  </action>
  <verify>Check src/index.ts</verify>
  <done>Configs are exported</done>
</task>

</tasks>

<verification>
Check exports in index.ts.
</verification>

<success_criteria>
- [ ] recommended.ts created
- [ ] strict.ts created
- [ ] index.ts updated
</success_criteria>
