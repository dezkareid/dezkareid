---
phase: 02-core-web-rules
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [src/rules/no-deprecated-html.ts, src/rules/require-img-alt.ts, src/rules/no-inline-event-handlers.ts, src/index.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "no-deprecated-html rule exists"
    - "require-img-alt rule exists"
    - "no-inline-event-handlers rule exists"
  artifacts:
    - path: "src/rules/no-deprecated-html.ts"
      provides: "Rule logic for deprecated tags"
    - path: "src/rules/require-img-alt.ts"
      provides: "Rule logic for image alt text"
    - path: "src/rules/no-inline-event-handlers.ts"
      provides: "Rule logic for inline events"
---

<objective>
Implement the 3 core web rules for both HTML and JSX.

Purpose: Achieve phase goal of essential web linting.
Output: 3 rule implementation files.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-web-rules/02-CONTEXT.md
@.planning/phases/02-core-web-rules/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement no-deprecated-html rule</name>
  <files>src/rules/no-deprecated-html.ts</files>
  <action>
    Create rule targeting `<center>`, `<font>`, `<big>`, `<strike>`, `<tt>`.
    Support both HTML (via Tag nodes) and JSX (via JSXOpeningElement).
  </action>
  <verify>Check file existence</verify>
  <done>Rule is implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement require-img-alt rule</name>
  <files>src/rules/require-img-alt.ts</files>
  <action>
    Enforce `alt` attribute on `<img>`.
    Per CONTEXT: `alt=""` and `aria-hidden="true"` MUST still trigger a warning.
    Support both HTML and JSX.
  </action>
  <verify>Check file existence</verify>
  <done>Rule is implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement no-inline-event-handlers rule</name>
  <files>src/rules/no-inline-event-handlers.ts</files>
  <action>
    Disallow `on*` attributes in HTML and JSX (e.g., `onclick`, `onClick`).
  </action>
  <verify>Check file existence</verify>
  <done>Rule is implemented</done>
</task>

<task type="auto">
  <name>Task 4: Export rules in index.ts</name>
  <files>src/index.ts</files>
  <action>
    Add the 3 new rules to the plugin object.
  </action>
  <verify>Check src/index.ts</verify>
  <done>Rules are exported</done>
</task>

</tasks>

<verification>
Check file contents for logic alignment with research.
</verification>

<success_criteria>
- [ ] 3 rule files created
- [ ] index.ts updated
</success_criteria>
