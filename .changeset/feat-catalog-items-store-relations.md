---
"@dezkareid/collectstory": minor
---

Add public catalog with store relations. Introduces a global admin-managed catalog of collectible items linked to stores, a "Where to Buy" section on item detail pages, an autocomplete picker for linking collection items to catalog items, and public `/catalog` and `/catalog/[slug]` pages with ISR and schema.org structured data.
